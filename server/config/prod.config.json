{
    
    "port": 9092,
    // server端配置，当前的web服务会根据此配置去请求服务端的接口
    
    // if useProxy is true, will proxy server config
    "useProxy": false,
    "isHttps": false,

    "proxyServer": {
        "host": "ex.bitbal.top",
        "port": "80",
    }
    
}